development:
  adapter: redis
  url: redis://localhost:6379/1

test:
  adapter: async

production:
  adapter: redis
  port: 6379
  url: redis://<%= ENV['REDIS_HOST'] %>
  # channel_prefix: my_app_production